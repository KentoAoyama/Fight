using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStatusManager : MonoBehaviour
{
    //[Header("Status")]
    //[SerializeField] float _playerHp = 1000;



    void Start()
    {
        //PlayerStateManager a = GetComponent<PlayerStateManager>();

        ////メンバー変数 : クラスのインスタンスに属する
        //a.bbb = PlayerStateManager.PlayerGuard.IsGuard;

        ////スタティック変数 : プログラム実行中(実行時から終了時まで)にメモリ空間上に確保されている
        //PlayerStateManager.kakak = PlayerStateManager.PlayerGuard.IsGuard;
    }


    void Update()
    {

    }
}
